#include "temp.h"

// Enums are just a way to name values
// Enums are integers

// You just want to use them for checking purposes

// Problem is they are not grouped, so you will become confused

int A = 0;
int B = 1;
int C = 2;

int main(void){
    int value = 0;

    if(value == A){
        logn("Equal")
    }
    if(value != B){
        logn("Not Equal")
    }

    return 0;
}