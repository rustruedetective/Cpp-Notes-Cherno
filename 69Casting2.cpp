#include "temp.h"

// C++ has a number of casts:-
// Static cast
// Reinterpret cast
// Dynamic cast
// Const cast
/*
    These above guys dont do anything extra
    they are just syntactic sugar

    biggest reason for them
    to search for them in source code...haha
    you coudnt have searched the simple c casts without regex...lol

    e.g dynamic cast returns null if unsuccessful
    e.g static cast only checks some runtime stuff like if the conversion is possible or not
    e.g reinterpret cast puts into english words type punning
    e.g const cast means removing or adding const
*/

// C++ style cast (static cast):-
// Casting:- static_cast<datatype2>(datatype1Variable)
// Casting:- static_cast<datatype2>(datatype1Variable + datatype1Variable2)

int main(void){
    // C++ style cast/aka static cast:-
    

    return 0;
}